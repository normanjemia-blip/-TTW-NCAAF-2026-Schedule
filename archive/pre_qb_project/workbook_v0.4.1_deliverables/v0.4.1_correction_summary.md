# TTW NCAAF Power Ratings 2026 — v0.4.1 Correction Build Summary

Base: `v0.4` (preserved unchanged in `workbook_v0.4_deliverables/`).
Approved `v0.3.1`: untouched (verified — the build script never opens it
for writing; the diff below is computed against it read-only).
Phase 5: not started. **Status: pending independent approval.**

## 1. TTW independent 2025 prior — investigation before declaring blocked

Full report: `ttw_prior_source_investigation.md` (with all probe evidence
archived under `cfbfastr_probe/`). One-paragraph result:

The official public distribution (GitHub release assets on
`sportsdataverse/sportsdataverse-data`, tag `cfbfastR_cfb_pbp`, per the
cfbfastR loader's own source code) **does contain a complete final 2025
season** — 1,662 games including the full postseason and both
reclassifiers (NDSU 13 games, Sacramento State 12), per the repo's
publicly readable manifest — and the dataset family's schema (verified
from the same repo's accessible 2019 parquet: 368 columns) **carries every
field the approved methodology requires** (per-play off/def EPA, success,
scores, teams, game_id, week; neutral-site/date join via ESPN `game_id`
from the already-approved Phase 3 schedule source). The 2025 asset itself
is **blocked by this session's platform**: release-asset downloads outside
the session's repo scope are gated, and `add_repo` was denied
("cross-tier adds are not supported in v1"). The prior therefore stays
blank — an access limitation, precisely documented; no proxy substituted,
no methodology change. Unblock paths: a session with a sportsdataverse
repo as initial source; a user-supplied `play_by_play_2025` file (~70 MB);
or a CFBD API key.

## 2. Stale visible text corrected (4 cells, text only)

| Cell | Was | Now |
|---|---|---|
| `START HERE!A3` | "PHASE 3 STATE: … Ratings … still not loaded" | States FPI + TeamRankings loaded (138 teams, raw+date+cite, blend renormalizes over them); lists exactly what remains unavailable/unpopulated: SP+, TTW prior, VSiN, QB values, market lines, in-season statistics |
| `PRESEASON!A1` | "(VALUES EMPTY UNTIL PHASE 4)" | "(FPI + TeamRankings LOADED 2026-07-19; SP+/TTW-prior/VSiN input columns blank — sources unavailable, see CHANGELOG)" |
| `SETTINGS!C22:C23` | "BLANK until Phase 4" | "BLOCKED/DEFERRED — … until verified in-season EPA data is available" |
| `FCS TIERS!A2` | "…BLANK until Phase 4 populates verified FCS rating data…" | "…verified FCS ratings are deferred pending an authorized source load…" (no Phase 4 promise) |

## 3. Phase 4 summary corrected (in `workbook_v0.4_deliverables/`)

- CHANGELOG change-scope figure corrected: **9 rows / 36 cells** (was
  wrongly stated as 6 rows / 24 cells — the original text only counted the
  first batch of entries, not the three user-decision entries added at
  finalization).
- "838 sheets" typo replaced with the correct description: **all 21
  sheets**, every row/column, ArrayFormula compared by ref+text.
- Blocked-source description rephrased to name only the **manual-input
  cells** as blank (SP+ D/E/F, TTW L/N/O, VSiN U/V/W; QB VALUES C:F, H:L)
  — the associated formula columns (G,K,M,P,T,X,Y:AC; QB A,B,G,M) are
  intact, unchanged, and merely render blank while inputs are missing.

## 4. Audit artifacts included in this directory

- ESPN FPI: `raw_sources/espn_fpi_2026preseason_20260719.html` (raw page),
  `raw_sources/espn_fpi_2026preseason_20260719_parsed.json` (extracted
  embedded JSON), `fpi_matched.json` (abbrev → name → value).
- TeamRankings: `raw_sources/teamrankings_predictive_2026preseason_20260719.html`
  (raw page), `tr_teams_dump.txt` (parsed rank/name/rating),
  `tr_matched.json` (abbrev → rank/name/rating), and
  `match_teamrankings.py` — the explicit, manually verified 138-entry
  name→abbreviation mapping table with the matching/validation logic.
- SP+ evidence of paywall: `raw_sources/espn_spplus_article_PAYWALLED_20260719.html`.
- TTW-prior probe evidence: `cfbfastr_probe/` (loader source, README,
  games manifest, 2019 schema column list, probe log).
- **Complete diff report:** `v0.4.1_vs_v0.3.1_full_diff.tsv` — every
  changed cell (sheet, cell, v0.3.1 value, v0.4.1 value), 882 rows.
- Validation battery: `validate_v041.py` + `validation_report.txt` (its
  full PASS/FAIL output).

## 5. Validation battery result (all 21 checks PASS)

- **Source revalidation (user item 5):** both archived raw sources were
  independently re-parsed (fresh regex/JSON extraction, not reusing the
  load-time intermediates) and compared cell-by-cell against the workbook:
  FPI 138/138 exact, TeamRankings 138/138 exact. **Zero discrepancies —
  values preserved unchanged.**
- **Diff scope:** exactly 5 sheets touched vs v0.3.1 — START HERE (2
  cells: banner + A3), PRESEASON (829 = 828 data cells + A1 header),
  SETTINGS (2: C22, C23), FCS TIERS (1: A2), CHANGELOG (48 = 12 rows ×
  4: nine v0.4 + three v0.4.1 entries). Nothing else.
- **Formula integrity:** zero cells containing a formula in v0.3.1 differ
  in v0.4.1 (asserted over every cell of all 21 sheets).
- **Blank-input assertions:** SP+/TTW/VSiN raw+date+cite input cells,
  win-total AD/AE, and all QB VALUES manual-input columns verified fully
  blank.
- **Structural:** 888 schedule game ids intact; IMPORT SCHEDULE dims
  A1:N1005; CHANGELOG contains exactly 9 v0.4 + 3 v0.4.1 entries.

## 6. What v0.4.1 does NOT do

No preseason value changed (FPI/TR revalidated and preserved). No formula
added, removed, or edited. No QB, VSiN, SP+, TTW-prior, market-line, or
stats data loaded. No SETTINGS constant populated. v0.3.1 and v0.4 files
untouched. Phase 5 not started.
