# TTW NCAAF Power Ratings 2026 — Phase 4 Source Load Summary (v0.4, in progress)

Base workbook: approved `v0.3.1` (read-only; not modified — see
`workbook_v0.3.1_deliverables/`). This file, `v0.4_working.xlsx`, is a
separate working copy created for Phase 4 (`md5sum` confirmed identical to
v0.3.1 at creation time, before any Phase 4 edit was made).

**Status: Phase 4 data loading is partially complete.** Two of five
preseason rating sources were verified accessible and loaded. Three sources
are blocked and are documented below rather than silently substituted or
fabricated, per Architecture Amendment v0.2 §1 ("if a reliable independent
TTW 2025 prior cannot be constructed, the build stops and reports the
limitation") and the project's standing "verified sources only" rule.

## 1. Sources tested and their outcome

| Source | Status | Detail |
|---|---|---|
| ESPN FPI | **Loaded** | 138/138 teams, real 2026 preseason values (not placeholder/zero records — W-L shown as 0-0, confirming a genuine preseason snapshot) |
| TeamRankings (Predictive) | **Loaded** | 138/138 teams, real 2026 preseason values |
| ESPN SP+ | **Blocked** | Insider-paywalled; only a prose article with scattered individual team ranks is public, not a complete structured 138-team table |
| TTW independent 2025 prior | **Blocked** | Requires a CollegeFootballData.com (CFBD) API key; unauthenticated request returned `401 Unauthorized`; no key is configured in this environment |
| VSiN | **Blocked** | Explicitly documented as user-supplied-only in the architecture docs (enhancement, never a prerequisite); none has been provided |
| QB starters / depth charts | **Blocked** | ESPN depth-chart endpoints return an empty body this early in the offseason (tested 2026-07-19); not a fetch error, just not yet published |

## 2. FPI load detail

- Source: `https://www.espn.com/college-football/fpi` (structured JSON
  embedded in the rendered page, `window['__espnfitt__']`, not scraped
  HTML text). Page's own `lastUpdated` timestamp: `2026-06-30T08:00Z`.
  Retrieved: 2026-07-19.
- Raw HTML and the extracted JSON are archived in
  `raw_sources/espn_fpi_2026preseason_20260719.html` /
  `..._parsed.json`.
- Team matching: FPI uses ESPN's own team-name database, so the existing
  TEAM MAP `K:L` ESPN-display-name alias table (built in Phase 3 for the
  schedule load) was reused directly — no new fuzzy matching needed.
  Result: **138/138 matched, 0 unmatched, 0 ambiguous.**
- Matched data (abbrev -> ESPN display name -> FPI value): `fpi_matched.json`.
- Loaded into `PRESEASON!H:J` (raw / date / cite) for rows 6:143, one row
  per team, aligned 1:1 with `TEAM MAP` row order (same convention as the
  Phase 3 schedule load).

## 3. TeamRankings load detail

- Source: `https://www.teamrankings.com/college-football/ranking/predictive-by-other`
  (server-rendered HTML `<table>`, not an API — but a structured table, not
  prose). Retrieved: 2026-07-19.
- Raw HTML archived in
  `raw_sources/teamrankings_predictive_2026preseason_20260719.html`.
- Team matching: TeamRankings uses its own, non-ESPN abbreviation
  convention (e.g. "N Illinois", "S Florida", "J Madison", "Mississippi"
  for Ole Miss). Every one of the 138 TeamRankings names was manually
  checked against the canonical `TEAM MAP` list and mapped to its
  abbreviation — see `tr_teams_dump.txt` (raw parsed rank/name/rating) and
  the `TR_TO_ABBREV` table in the load script. Result: **138/138 matched,
  0 unmatched, 0 canonical abbreviations left without a match.**
  Disambiguation-prone cases were individually verified, not
  pattern-guessed:
  - Directional prefixes: "N Texas" -> North Texas (UNT) vs. "N Illinois"
    -> Northern Illinois (NIU); "S Florida" -> South Florida (USF) vs.
    "S Alabama" -> South Alabama (USA); "E Carolina"/"E Michigan"/
    "W Michigan"/"C Michigan" -> East/Eastern/Western/Central, checked
    individually since the prefix expands inconsistently.
  - "Mississippi" -> Ole Miss (MISS) vs. "Mississippi St" -> Mississippi
    State (MSST) — confirmed both present as distinct rows.
  - "J Madison" -> James Madison (JMU); "UL Monroe" -> ULM; "Sam Houston"
    and "UMass" already matched their canonical names directly.
- Matched data: `tr_matched.json`.
- Loaded into `PRESEASON!Q:S` (raw / date / cite) for rows 6:143.

## 4. What was intentionally left blank

`PRESEASON!D:F` (SP+), `L:P` (TTW independent 2025 prior), and `U:X`
(VSiN) remain blank for all 138 teams — no placeholder, no zero, no
proxy value substituted. `QB VALUES` remains entirely unpopulated (its
existing formula columns already handle blank/UNCERTAIN gracefully; no
change was made to that tab this session).

## 5. Formula behavior verified with only 2 of 5 sources loaded

The `PRESEASON` tab's existing (pre-Phase-4, unmodified) formula columns
implement the approved "missing is not zero — renormalize over available
sources" rule (`Y`: available weight, `Z`: final prior, `AA`: source
count, `AB`: single-source flag). With only FPI (weight 0.25) and
TeamRankings (weight 0.15) populated, every one of the 138 rows now
computes:
- Available weight `Y` = 0.40 (0.25 + 0.15) for all 138 rows (constant,
  since FPI and TR have no per-team gaps).
- Source count `AA` = 2 for all 138 rows; single-source flag `AB` blank
  for all 138 (correctly not flagged, since 2 sources qualifies as
  multi-source).
- Final prior `Z` recomputed as the weight-renormalized blend of just
  FPI-norm and TR-norm.
- Sanity check on the resulting blended ranking: top 5 = Ohio State,
  Texas, Notre Dame, Oregon, Georgia; bottom 5 = UMass, Sam Houston, ULM,
  Kent State, Ball State — consistent with both sources' individual
  preseason rankings.

This was verified by independently reproducing the exact formula text in
Python against the loaded values (not by guessing) — see the diff/parity
check described in Section 6.

## 6. Change scope verification

A full cell-by-cell diff of `v0.4_working.xlsx` against approved `v0.3.1`
(838 sheets... i.e., every sheet, all rows/columns, `ArrayFormula` objects
compared by their `.ref`/`.text`, not Python object identity) shows changes
in exactly three places and nothing else:
- `START HERE!A1` — version banner text only.
- `PRESEASON` — exactly 828 cells = 138 teams × 6 columns
  (`H,I,J,Q,R,S`), rows 6-143 only. No other `PRESEASON` cell (including
  every formula column) changed.
- `CHANGELOG` — 24 cells = 6 new rows × 4 columns, appended after the
  existing v0.3.1 entries.

No formula was added, removed, or edited anywhere in the workbook.
`v0.3.1_with_schedule.xlsx` itself was never opened for writing this
session — only read, to build the v0.4 copy.

## 7. Blockers requiring the user's decision before Phase 4 can be
   considered complete

1. **SP+**: renormalize the blend over FPI + TeamRankings only (current
   default behavior, already verified above), or does the user have
   ESPN Insider/ESPN+ access or a manually-supplied complete SP+ list?
2. **TTW independent 2025 prior**: does the user have a CollegeFootballData.com
   API key to supply, or should this prior stay undocumented/blocked for
   now, per the architecture doc's explicit instruction not to substitute
   2025 SP+ or another proxy?
3. **QB VALUES**: stay blank/UNCERTAIN until depth charts are closer to
   published (the existing formula system already tolerates this), or does
   the user want a different interim approach?
4. **VSiN**: does the user have VSiN numbers ready to supply now?

No further Phase 4 work (QB VALUES population, any TTW-prior construction,
or anything beyond Phase 4) will proceed until the user responds to these,
per their instruction to document every material decision and stop for
approval before advancing beyond Phase 4.
